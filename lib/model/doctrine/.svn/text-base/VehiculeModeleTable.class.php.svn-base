<?php

/**
 * VehiculeModeleTable
 * 
 * This class has been auto-generated by the Doctrine ORM Framework
 */
class VehiculeModeleTable extends Doctrine_Table
{
    /**
     * Returns an instance of this class.
     *
     * @return object VehiculeModeleTable
     */
    public static function getInstance()
    {
        return Doctrine_Core::getTable('VehiculeModele');
    }

    /*
     * retourne une jointure pour optimiser les requetes
     */
    public function retrieveBackendVehiculeMarqueList(Doctrine_Query $q)
    {
        $rootAlias = $q->getRootAlias();
        $q->leftJoin($rootAlias . '.VehiculeMarque c');
        return $q;
    }
    
    /*
     * récupere la liste des vehicules pour une marque donnée
     */
    public function getVehiculeModeleListByMarque($idMarque = null)
    {
        
            $q = Doctrine_Query::create()
                ->select('id_modele,nom_modele')
                ->from('VehiculeModele')
                ->where('id_marque = ?',$idMarque)
                    ;
        return $q->fetchArray();
    }
    
    /*
     * récupere la liste des vehicules pour une marque donnée
     */
    public function getVehiculeModeleListByMarqueQuery($idMarque = null)
    {
        
            $q = Doctrine_Query::create()
                ->select('id_modele,nom_modele')
                ->from('VehiculeModele')
                ->where('id_marque = ?',$idMarque)
                    ;
        return $q;
    }

}